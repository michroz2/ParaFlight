1. Идем маленькими шагами. Меняем только одну функциональность на каждом шаге.
2. НИКОГДА не изменять то, что уже работает, без явного согласия пользователя.
3. НИКОГДА не удалять работающий код ради оптимизации или эстетики.
4. Перед написанием кода всегда обсуждать предлагаемые изменения, предоставлять Implementation Plan и ждать утверждения.
5. ВСЕ комментарии в коде пишутся ИСКЛЮЧИТЕЛЬНО на русском языке.
6. ВСЕ commit messages (сообщения коммитов) пишутся ИСКЛЮЧИТЕЛЬНО на русском языке.
7. Каждый commit message ДОЛЖЕН начинаться с префикса текущей версии в формате 'vX.X: ' (например, 'v0.1: Добавлен экран').
8. ВЕРСИОНИРОВАНИЕ И СБОРКА: Перед каждой новой сборкой или завершением логического этапа, версия приложения в файле `pubspec.yaml` (поле `version:`) ДОЛЖНА БЫТЬ АВТОМАТИЧЕСКИ обновлена ИИ-ассистентом (по правилам семантического версионирования). Не спрашивайте разрешения у пользователя на обновление версии в `pubspec.yaml` — просто обновляйте её самостоятельно и фиксируйте коммитом. Данные в titlebar интерфейса подтягиваются оттуда автоматически.
